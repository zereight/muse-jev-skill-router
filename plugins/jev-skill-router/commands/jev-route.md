# /jev-route

Recommend muse skills for the given prompt using the jev-router plugin.

Run `python3 scripts/jev-route.py "<prompt>" --top 5` inside the
`jev-skill-router` plugin directory, then read only the recommended
`SKILL.md` files.
